{
  "name": "claudeview-memory",
  "version": "0.1.5",
  "description": "CLI tool for Claude Code session introspection — search, browse, and drill into past sessions",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "bin": {
    "claudeview-memory": "./dist/cli.js"
  },
  "files": [
    "dist/",
    "skill/"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile src/cli.ts --outfile claudeview-memory",
    "build:npm": "node scripts/build-npm.mjs",
    "prepublishOnly": "node scripts/build-npm.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "bun-types": "latest",
    "esbuild": "^0.27.3"
  }
}
